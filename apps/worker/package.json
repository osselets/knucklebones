{
  "name": "@knucklebones/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "module": "./dist/index.mjs",
  "scripts": {
    "db:migrate:local": "wrangler d1 migrations apply PLAYERS_DB --local",
    "db:reset:local": "node ./scripts/reset-local-state.mjs && pnpm db:migrate:local",
    "dev": "NODE_ENV=development wrangler dev",
    "dev:test": "pnpm db:migrate:local && NODE_ENV=development wrangler dev"
  },
  "dependencies": {
    "@knucklebones/common": "workspace:*",
    "itty-durable": "^2.4.1",
    "itty-router": "^5.0.24",
    "toucan-js": "^4.1.1"
  }
}
